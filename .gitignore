# macOS
.DS_Store

# Wrangler / Cloudflare local state
.wrangler/

# Node (only present if wrangler is installed locally)
node_modules/

# Playwright MCP scratch output
.playwright-mcp/

# Claude Code skills (local tooling, not site content)
.claude/
.agents/
skills-lock.json

# Impeccable design context (local)
PRODUCT.md
DESIGN.md
.impeccable/
.codex/
